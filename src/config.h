#ifndef CONFIG_H
#define CONFIG_H

// --- 统一的 painlessMesh 网络配置 (所有节点必须一致) ---
#define MESH_NETWORK_SSID      "MySuperMesh"
#define MESH_NETWORK_PASSWORD  "ThisIsTheMeshPassword"
#define MESH_PORT              5555

// --- 外部路由器配置 (只有根节点需要连接) ---
#define EXTERNAL_ROUTER_SSID "Gionix"
#define EXTERNAL_ROUTER_PASS "u6t4z9ip"

#endif // CONFIG_H